# Кастомные спрайты

PNG-файлы в этой папке переопределяют спрайты игры: при наличии файла с именем
роли используется он, иначе — спрайт по умолчанию из атласа 0x72 DungeonTilesetII.

## Формат

- Имя файла задаёт роль (таблица ниже): `<роль>.png` — статичный спрайт,
  `<роль>.N.png` — анимация из N кадров, расположенных в изображении
  горизонтально слева направо (ширина кадра = ширина изображения / N).
- PNG с альфа-каналом.
- Базовая клетка — 32×32 px. Спрайты персонажей могут превышать клетку —
  якорь находится в середине её нижней грани. Роли `floor`, `wall` и `ladder`
  масштабируются ровно в клетку.

## Роли

| Файл          | Назначение        |
|---------------|-------------------|
| `player.png`  | Игрок (Juggernaut) |
| `pudge.png`   | Враг: Pudge — медленный танк |
| `bloodseeker.png` | Враг: Bloodseeker — крадёт максимум HP |
| `riki.png`    | Враг: Riki — блинки и невидимость |
| `axe.png`     | Враг: Axe — двойной шаг и контратака |
| `skywrath.png` | Враг: Skywrath Mage — усыпляет, ходит по диагонали |
| `food.png`    | Предмет: еда      |
| `elixir.png`  | Предмет: эликсир  |
| `scroll.png`  | Предмет: свиток   |
| `sword.png`   | Предмет: оружие на полу |
| `coin.png`    | Сокровища         |
| `floor.png`   | Тайл травы (пол комнат) |
| `path.png`    | Тайл тропы (коридоры) |
| `wall.png`    | Тайл чащи (стены, туман, пустота) |
| `decor.png`   | Кустики, изредка рисуются на траве |
| `tree.png`    | Деревья, изредка рисуются в чаще |
| `ladder.png`  | Выход на следующий уровень |

Для тайловых ролей (`floor`, `path`, `wall`, `ladder`) кадры мультикадрового PNG —
пространственные варианты: выбираются по хешу координат клетки, а не по времени.

## Источники

Анимированные спрайты героев (`player.8.png`, `pudge.8.png`, `bloodseeker.21.png`,
`ghost.8.png`, `axe.48.png`, `skywrath.5.png`) — пиксельные Showcase Decorations
из Dota 2 (© Valve), взяты с Liquipedia. Используются как фан-контент
в некоммерческом проекте.

Шрифт и базовый тайлсет — см. `../LICENSE.txt`.
